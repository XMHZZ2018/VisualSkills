app_name: "Zotero"
app_version: "7"
domain: "zotero"

sources:
  html_guide:
    root_url: "https://www.zotero.org/support/start"
    # Depth-1 = parse only the root page; depth-2 also harvests sub-headings
    # from each linked page so the taxonomy can reach level-3 sub-topics.
    crawl_depth: 2
